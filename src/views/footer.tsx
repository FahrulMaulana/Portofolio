import { Footer } from "@/components/blocks/footer-section";

export default function FooterFix() {
    return (
        <footer className="w-full mt-auto bottom-0 left-0 right-0">
            <Footer />
        </footer>
    );
}